const restaurants = require('./mock.data/restaurants');
const users = require('./mock.data/users');
const orders = require('./mock.data/orders');

module.exports = {
    restaurants,
    users,
    orders
};